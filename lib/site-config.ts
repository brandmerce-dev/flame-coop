/**
 * Single source of truth for the canonical site URL.
 * Used in: sitemap, robots, layout metadata, JSON-LD, and per-page canonical tags.
 * Update this value when deploying for a new co-op client.
 */
export const SITE_URL = 'https://www.theflamechristiancooperative.com';
